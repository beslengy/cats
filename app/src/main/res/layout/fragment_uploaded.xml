<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.molchanov.cats.viewmodels.uploaded.UploadedViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.uploaded.UploadedFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_uploaded"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="@dimen/rv_item_margin"
            android:paddingTop="@dimen/rv_item_margin"
            android:paddingEnd="@dimen/rv_item_margin"
            android:scrollbars="vertical"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:listData="@{viewModel.uploadedImages}"
            app:spanCount="2"
            tools:listitem="@layout/image_item" />

        <ImageView
            android:id="@+id/status_image_uploaded"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:ApiStatusImage="@{viewModel.status}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/status_text_uploaded"
            tools:ignore="ContentDescription"/>

        <TextView
            android:id="@+id/status_text_uploaded"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="22sp"
            app:ApiStatusText="@{viewModel.status}"
            app:layout_constraintBottom_toTopOf="@id/status_image_uploaded"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed"
            tools:text="Status" />



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>